// JS entry is intentionally empty: the app talks to the native module through
// NativeModules.BlahNeuralTts directly (see src/lib/neuralTts.ts). This package
// exists so React Native autolinking compiles the iOS podspec.
module.exports = {};
